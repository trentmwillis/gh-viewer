<section class="hero is-primary">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        {{model.name}}
      </h1>

      <h2 class="subtitle">
       {{#if model.description}}
         {{model.description}} |
        {{/if}}
        {{model.html_url}}
      </h2>
    </div>
  </div>
</section>

{{outlet}}
